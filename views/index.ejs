<h2 class="page-header">Recently Added</h2>
<div>
    <% books.forEach(book => { %>
        <a href="books/<%= book.id %>">
        <img class="book-image" height="150" width="100" src="<%= book.coverImagePath %>" />
        </a>
        <% }) %>
</div>